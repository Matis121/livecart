<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="mb-6">
    <%= link_to employees_path, class: "btn btn-ghost btn-sm gap-2" do %>
      <%= lucide_icon('arrow-left', class: 'w-4 h-4') %>
      Powrót
    <% end %>
  </div>
  <h1 class="text-3xl font-bold mb-6">Nowy pracownik</h1>
  <%= render "form" %>
</div>
