<div class="container mx-auto px-4 py-6 sm:py-8 max-w-7xl">
  <!-- Header -->
  <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-3 mb-6">
    <h1 class="text-2xl sm:text-3xl font-bold">Pracownicy</h1>
    <%= link_to "Nowy pracownik", new_employee_path, class: "btn btn-primary w-full sm:w-auto" %>
  </div>
  <% if @users.any? %>
    <%= render "users/table" %>
  <% else %>
    <%= render "users/empty_state" %>
  <% end %>
</div>
